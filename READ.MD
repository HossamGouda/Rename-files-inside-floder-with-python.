This is simple Python file to rename a bulk files inside folder (remove the numbers from the begaining of each file name
)
you can rename files in folder just mention the path of your files and set the name 
then run the function 